# fmod-midi-to-markers
Converts a multi-track MIDI file into FMOD markers

Add all files to the `/Scripts` folder in your project directory. The folder will need to be created if it doesn't exist already.
